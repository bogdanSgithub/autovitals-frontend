/**
 * Interface representing a car.
 * @interface Car
 */
export interface Car {
    _id: string;
    model: string;
    year: number;
    mileage: number;
    dateBought: Date;
    url: string;
    userID: string;
  }
  